<Window x:Class="CPS_WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CPS_WPF"
        mc:Ignorable="d"
        Title="CPS Tester" Height="235" Width="150"
        ResizeMode="NoResize">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Button Name="Button_Click" Margin="10" Grid.Row="0" Width="100" Height="100" Click="button_CPSPressed">Click here!</Button>
        <TextBlock Name ="TextBlock_CPS"  Margin="10" Grid.Row="1">CPS: </TextBlock>
        <Button Name="Button_Reset"  Margin="10" Grid.Row="2" Width="100" Click="button_ResetPressed">Reset</Button>
        
    </Grid>
</Window>
